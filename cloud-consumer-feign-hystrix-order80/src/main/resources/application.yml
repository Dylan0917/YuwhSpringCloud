server:
  port: 80

eureka:
  client:
    register-with-eureka: false
    service-url:
      defaultZone: http://activate.xmind.net:7001/eureka/ #入驻地址 不集群
#客户端降级 在主启动类注解开启
#feign:
#  hystrix:
#    enabled: true #在feign中开启hystrix